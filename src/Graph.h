#include <list>

using namespace std;
using std::string;

class Graph {
    public: 
        // Graph(string nodes_file, string edge_file);

    private: 
        // // adjacency list with weights. each node is an airport, each edge is a route
        // list<string> * adjList;
};